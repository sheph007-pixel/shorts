# Force Python provider for Railway
[phases.setup]
nixPkgs = ["python311", "ffmpeg-full"]

[phases.install]
dependsOn = ["setup"]
cmds = ["cd backend && pip install -r requirements.txt"]

[start]
cmd = "cd backend && python app.py"
