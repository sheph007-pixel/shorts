# Railway/Nixpacks configuration for Python backend
[phases.setup]
nixPkgs = ["python311", "ffmpeg-full", "libsndfile"]

[phases.install]
dependsOn = ["setup"]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python app.py"
